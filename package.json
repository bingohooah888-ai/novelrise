{
  "type": "module",
  "scripts": {
    "test": "node --test",
    "lint": "eslint \"api/**/*.js\" \"tests/**/*.js\" \"tests/**/*.mjs\"",
    "format": "prettier --write \"api/**/*.js\" \"tests/**/*.{js,mjs,json}\" \"*.json\" \".github/workflows/*.yml\" \"eslint.config.mjs\"",
    "format:check": "prettier --write \"tests/vercel-admin-allowlist.test.mjs\" && git diff -- tests/vercel-admin-allowlist.test.mjs && exit 1",
    "syntax:check": "node scripts/check-js-syntax.mjs",
    "runtime:gate": "node scripts/runtime-gate-entry.mjs",
    "preflight": "npm run preflight:fast",
    "preflight:agent": "npm run runtime:gate -- --phase=implementation && npm run preflight:fix",
    "preflight:fast": "npm run format:check && npm run lint && npm test && npm run syntax:check && npm run preflight:merge-readiness && git diff --check",
    "preflight:fix": "npm run format && npm run lint && npm test && npm run syntax:check && npm run preflight:merge-readiness && git diff --check",
    "preflight:merge-readiness": "node scripts/check-merge-readiness.mjs",
    "preflight:db": "bash scripts/run-rls-integration.sh",
    "preflight:e2e": "npm --prefix tests/e2e test",
    "preflight:full": "npm run preflight:fast && npm run preflight:db && npm run preflight:e2e"
  },
  "engines": {
    "node": "24.x"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.57.0",
    "stripe": "^20.4.0"
  },
  "devDependencies": {
    "eslint": "^10.8.1",
    "prettier": "^3.0.0"
  }
}
