{
  "name": "novelight-browser-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:staging": "playwright test --config=playwright.staging.config.mjs"
  },
  "engines": {
    "node": "24.x"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1"
  }
}
