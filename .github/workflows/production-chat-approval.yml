name: NOVELIGHT Chat-Mediated Production Approval

on:
  issue_comment:
    types: [created]

permissions:
  contents: read
  id-token: write
  issues: write

concurrency:
  group: novelight-production-chat-approval
  cancel-in-progress: false

jobs:
  remediate:
    name: Execute one scoped chat-approved Production remediation
    if: >-
      github.event.issue.pull_request == null &&
      github.event.issue.user.login == 'github-actions[bot]' &&
      startsWith(github.event.issue.title, '[Production Approval] billing-') &&
      github.event.comment.user.login == 'bingohooah888-ai' &&
      github.event.comment.author_association == 'OWNER' &&
      startsWith(github.event.comment.body, 'NOVELIGHT_PRODUCTION_APPROVE ')
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ github.token }}
      ISSUE_NUMBER: ${{ github.event.issue.number }}
      ISSUE_TITLE: ${{ github.event.issue.title }}
      ISSUE_BODY: ${{ github.event.issue.body }}
      REMEDIATION_URL: https://novelrise.vercel.app/api/production-billing-remediate
      EXPECTED_REMEDIATION_VERSION: chat-approval-oidc-v1
      OIDC_AUDIENCE: novelight-production-chat-approval

    steps:
      - name: Validate one-time scoped approval against its dedicated issue
        id: approval
        shell: bash
        env:
          APPROVAL_BODY: ${{ github.event.comment.body }}
        run: |
          set -euo pipefail

          approval_json="${APPROVAL_BODY#NOVELIGHT_PRODUCTION_APPROVE }"
          echo "$approval_json" | jq -e '
            type == "object" and
            .operation == "production-billing-remediation" and
            (.requestId | type == "string" and test("^billing-[0-9a-f]{40}-[0-9]+-[0-9]+$")) and
            (.mainSha | type == "string" and test("^[0-9a-f]{40}$")) and
            (.challenge | type == "string" and test("^[A-F0-9]{8}$")) and
            ((keys | sort) == (["challenge", "mainSha", "operation", "requestId"] | sort))
          ' >/dev/null || {
            echo 'Safety stop: invalid chat approval record.'
            exit 1
          }

          request_json="$(
            printf '%s\n' "$ISSUE_BODY" \
              | head -n 1 \
              | sed 's/^NOVELIGHT_PRODUCTION_REQUEST //' \
              | jq -c '.'
          )"
          echo "$request_json" | jq -e '
            type == "object" and
            .operation == "production-billing-remediation" and
            (.requestId | type == "string" and test("^billing-[0-9a-f]{40}-[0-9]+-[0-9]+$")) and
            (.mainSha | type == "string" and test("^[0-9a-f]{40}$")) and
            (.expiresAt | type == "string") and
            (.challenge | type == "string" and test("^[A-F0-9]{8}$")) and
            (.repairRequired | type == "boolean") and
            (.cleanupRequired | type == "boolean") and
            ((.repairRequired == false) or (.repairFingerprint | type == "string" and test("^sha256:[0-9a-f]{64}$"))) and
            ((.cleanupRequired == false) or (.cleanupFingerprint | type == "string" and test("^sha256:[0-9a-f]{64}$")))
          ' >/dev/null || {
            echo 'Safety stop: malformed Production request issue.'
            exit 1
          }

          request_id="$(echo "$request_json" | jq -r '.requestId')"
          request_sha="$(echo "$request_json" | jq -r '.mainSha')"
          request_challenge="$(echo "$request_json" | jq -r '.challenge')"
          approved_request_id="$(echo "$approval_json" | jq -r '.requestId')"
          approved_sha="$(echo "$approval_json" | jq -r '.mainSha')"
          approved_challenge="$(echo "$approval_json" | jq -r '.challenge')"

          test "$ISSUE_TITLE" = "[Production Approval] $request_id" || {
            echo 'Safety stop: request issue title does not match request ID.'
            exit 1
          }
          test "$approved_request_id" = "$request_id" || {
            echo 'Safety stop: approval request ID does not match this issue.'
            exit 1
          }
          test "$approved_sha" = "$request_sha" || {
            echo 'Safety stop: approval SHA does not match request SHA.'
            exit 1
          }
          test "$approved_challenge" = "$request_challenge" || {
            echo 'Safety stop: approval challenge does not match this one-time request.'
            exit 1
          }

          expires_at="$(echo "$request_json" | jq -r '.expiresAt')"
          expiry_epoch="$(date -u -d "$expires_at" +%s)" || {
            echo 'Safety stop: invalid request expiry.'
            exit 1
          }
          now_epoch="$(date -u +%s)"
          if [ "$now_epoch" -ge "$expiry_epoch" ]; then
            echo 'Safety stop: Production approval request expired.'
            exit 1
          fi

          curl --silent --show-error --fail \
            -H "Authorization: Bearer $GH_TOKEN" \
            -H 'Accept: application/vnd.github+json' \
            -H 'X-GitHub-Api-Version: 2022-11-28' \
            "https://api.github.com/repos/$GITHUB_REPOSITORY/issues/$ISSUE_NUMBER/comments?per_page=100" \
            > /tmp/request-comments.json
          comment_count="$(jq 'length' /tmp/request-comments.json)"
          [ "$comment_count" -lt 100 ] || {
            echo 'Safety stop: dedicated approval issue exceeded the bounded comment contract.'
            exit 1
          }
          used_count="$(
            jq --arg requestId "$request_id" '[
              .[]
              | select(
                  (.body | startswith("NOVELIGHT_PRODUCTION_CLAIMED ")) or
                  (.body | startswith("NOVELIGHT_PRODUCTION_CONSUMED "))
                )
              | select(.body | contains($requestId))
            ] | length' /tmp/request-comments.json
          )"
          if [ "$used_count" -ne 0 ]; then
            echo 'Safety stop: this Production approval request was already used.'
            exit 1
          fi

          current_main="$(
            curl --silent --show-error --fail \
              -H "Authorization: Bearer $GH_TOKEN" \
              -H 'Accept: application/vnd.github+json' \
              -H 'X-GitHub-Api-Version: 2022-11-28' \
              "https://api.github.com/repos/$GITHUB_REPOSITORY/git/ref/heads/main" \
              | jq -er '.object.sha'
          )"
          if [ "$current_main" != "$approved_sha" ]; then
            echo 'Safety stop: main changed after the user approved this scope.'
            exit 1
          fi

          repair_required="$(echo "$request_json" | jq -r '.repairRequired')"
          cleanup_required="$(echo "$request_json" | jq -r '.cleanupRequired')"
          repair_fingerprint="$(echo "$request_json" | jq -r '.repairFingerprint // empty')"
          cleanup_fingerprint="$(echo "$request_json" | jq -r '.cleanupFingerprint // empty')"
          if [ "$repair_required" != 'true' ] && [ "$cleanup_required" != 'true' ]; then
            echo 'Safety stop: request contains no Production mutation.'
            exit 1
          fi

          {
            echo "request_id=$request_id"
            echo "main_sha=$approved_sha"
            echo "repair_required=$repair_required"
            echo "repair_fingerprint=$repair_fingerprint"
            echo "cleanup_required=$cleanup_required"
            echo "cleanup_fingerprint=$cleanup_fingerprint"
          } >> "$GITHUB_OUTPUT"
          echo "Validated one-time chat approval for request $request_id at exact main $approved_sha."

      - name: Claim approval before any Production mutation
        shell: bash
        env:
          REQUEST_ID: ${{ steps.approval.outputs.request_id }}
          MAIN_SHA: ${{ steps.approval.outputs.main_sha }}
        run: |
          set -euo pipefail
          payload="$(jq -cn --arg requestId "$REQUEST_ID" --arg mainSha "$MAIN_SHA" --arg runId "$GITHUB_RUN_ID" '{requestId:$requestId,mainSha:$mainSha,runId:$runId}')"
          body="NOVELIGHT_PRODUCTION_CLAIMED $payload"
          curl --silent --show-error --fail \
            -X POST \
            -H "Authorization: Bearer $GH_TOKEN" \
            -H 'Accept: application/vnd.github+json' \
            -H 'X-GitHub-Api-Version: 2022-11-28' \
            "https://api.github.com/repos/$GITHUB_REPOSITORY/issues/$ISSUE_NUMBER/comments" \
            -d "$(jq -cn --arg body "$body" '{body:$body}')" \
            >/dev/null

      - name: Wait for exact Production remediation API version
        shell: bash
        run: |
          set -euo pipefail
          version=''
          for attempt in $(seq 1 36); do
            status="$(
              curl --silent --show-error \
                --output /tmp/remediation-version.json \
                --write-out '%{http_code}' \
                --connect-timeout 10 \
                --max-time 20 \
                "$REMEDIATION_URL"
            )"
            if [ "$status" = '200' ]; then
              version="$(jq -r '.remediationVersion // empty' /tmp/remediation-version.json)"
              if [ "$version" = "$EXPECTED_REMEDIATION_VERSION" ]; then
                echo "Production remediation API version is current: $version"
                break
              fi
            fi
            if [ "$attempt" -eq 36 ]; then
              echo "Safety stop: Production remediation API version did not converge; HTTP $status version=${version:-missing}."
              exit 1
            fi
            sleep 5
          done

      - name: Re-check approved main immediately before Production call
        shell: bash
        env:
          MAIN_SHA: ${{ steps.approval.outputs.main_sha }}
        run: |
          set -euo pipefail
          current_main="$(
            curl --silent --show-error --fail \
              -H "Authorization: Bearer $GH_TOKEN" \
              -H 'Accept: application/vnd.github+json' \
              -H 'X-GitHub-Api-Version: 2022-11-28' \
              "https://api.github.com/repos/$GITHUB_REPOSITORY/git/ref/heads/main" \
              | jq -er '.object.sha'
          )"
          test "$current_main" = "$MAIN_SHA" || {
            echo 'Safety stop: main changed before Production mutation.'
            exit 1
          }

      - name: Execute exact approved scope through GitHub OIDC
        shell: bash
        env:
          REQUEST_ID: ${{ steps.approval.outputs.request_id }}
          MAIN_SHA: ${{ steps.approval.outputs.main_sha }}
          REPAIR_REQUIRED: ${{ steps.approval.outputs.repair_required }}
          REPAIR_FINGERPRINT: ${{ steps.approval.outputs.repair_fingerprint }}
          CLEANUP_REQUIRED: ${{ steps.approval.outputs.cleanup_required }}
          CLEANUP_FINGERPRINT: ${{ steps.approval.outputs.cleanup_fingerprint }}
        run: |
          set -euo pipefail

          oidc_token="$(
            curl --silent --show-error --fail \
              -H "Authorization: bearer $ACTIONS_ID_TOKEN_REQUEST_TOKEN" \
              "${ACTIONS_ID_TOKEN_REQUEST_URL}&audience=${OIDC_AUDIENCE}" \
              | jq -er '.value'
          )"
          printf '::add-mask::%s\n' "$oidc_token"

          scope="$(
            jq -cn \
              --arg operation 'production-billing-remediation' \
              --arg requestId "$REQUEST_ID" \
              --arg mainSha "$MAIN_SHA" \
              --arg repairFingerprint "$REPAIR_FINGERPRINT" \
              --arg cleanupFingerprint "$CLEANUP_FINGERPRINT" \
              --argjson repairRequired "$REPAIR_REQUIRED" \
              --argjson cleanupRequired "$CLEANUP_REQUIRED" \
              '{operation:$operation,requestId:$requestId,mainSha:$mainSha,repairRequired:$repairRequired,repairFingerprint:(if $repairRequired then $repairFingerprint else null end),cleanupRequired:$cleanupRequired,cleanupFingerprint:(if $cleanupRequired then $cleanupFingerprint else null end)}'
          )"

          status="$(
            curl --silent --show-error \
              --output /tmp/remediation-result.json \
              --write-out '%{http_code}' \
              --connect-timeout 10 \
              --max-time 290 \
              -X POST \
              -H "Authorization: Bearer $oidc_token" \
              -H 'Content-Type: application/json' \
              --data "$scope" \
              "$REMEDIATION_URL"
          )"
          if [ "$status" != '200' ]; then
            echo "Production remediation API failed safely with HTTP $status."
            cat /tmp/remediation-result.json
            exit 1
          fi

          jq -e \
            --arg version "$EXPECTED_REMEDIATION_VERSION" \
            '.remediationVersion == $version and .ok == true and .noChargeWebhookProof == true and .finalIssueCount == 0' \
            /tmp/remediation-result.json >/dev/null || {
              echo 'Safety stop: Production remediation result did not satisfy completion gate.'
              cat /tmp/remediation-result.json
              exit 1
            }
          echo 'PASS: exact chat-approved Production remediation, no-charge external webhook proof, and final billing audit completed.'

      - name: Record consumed approval and close dedicated issue
        shell: bash
        env:
          REQUEST_ID: ${{ steps.approval.outputs.request_id }}
          MAIN_SHA: ${{ steps.approval.outputs.main_sha }}
        run: |
          set -euo pipefail
          payload="$(jq -cn --arg requestId "$REQUEST_ID" --arg mainSha "$MAIN_SHA" --arg runId "$GITHUB_RUN_ID" '{requestId:$requestId,mainSha:$mainSha,runId:$runId,result:"success"}')"
          body="NOVELIGHT_PRODUCTION_CONSUMED $payload"
          curl --silent --show-error --fail \
            -X POST \
            -H "Authorization: Bearer $GH_TOKEN" \
            -H 'Accept: application/vnd.github+json' \
            -H 'X-GitHub-Api-Version: 2022-11-28' \
            "https://api.github.com/repos/$GITHUB_REPOSITORY/issues/$ISSUE_NUMBER/comments" \
            -d "$(jq -cn --arg body "$body" '{body:$body}')" >/dev/null
          curl --silent --show-error --fail \
            -X PATCH \
            -H "Authorization: Bearer $GH_TOKEN" \
            -H 'Accept: application/vnd.github+json' \
            -H 'X-GitHub-Api-Version: 2022-11-28' \
            "https://api.github.com/repos/$GITHUB_REPOSITORY/issues/$ISSUE_NUMBER" \
            -d '{"state":"closed"}' >/dev/null

      - name: Close claimed request after a failed remediation
        if: failure() && steps.approval.outputs.request_id != ''
        shell: bash
        env:
          REQUEST_ID: ${{ steps.approval.outputs.request_id }}
        run: |
          set +e
          body="NOVELIGHT_PRODUCTION_FAILED {\"requestId\":\"$REQUEST_ID\",\"runId\":\"$GITHUB_RUN_ID\"}"
          curl --silent --show-error \
            -X POST \
            -H "Authorization: Bearer $GH_TOKEN" \
            -H 'Accept: application/vnd.github+json' \
            -H 'X-GitHub-Api-Version: 2022-11-28' \
            "https://api.github.com/repos/$GITHUB_REPOSITORY/issues/$ISSUE_NUMBER/comments" \
            -d "$(jq -cn --arg body "$body" '{body:$body}')" >/dev/null
          curl --silent --show-error \
            -X PATCH \
            -H "Authorization: Bearer $GH_TOKEN" \
            -H 'Accept: application/vnd.github+json' \
            -H 'X-GitHub-Api-Version: 2022-11-28' \
            "https://api.github.com/repos/$GITHUB_REPOSITORY/issues/$ISSUE_NUMBER" \
            -d '{"state":"closed"}' >/dev/null
